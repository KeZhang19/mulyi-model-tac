# Rotate-Bulb 场景与奖励续训更新（2026-09-12）

已同步本地场景和新奖励，服务器最终训练于北京时间 **12:42:26** 重启。
实际训练实例为 `ssh dsw-nin473ap6o7410zyra`，hostname 为 `dsw-912204-7d55cd968d-qfrrk`。
用户提供的 `dsw-8prvxfz53nbu67qzsu` 可访问相同项目目录，但它运行的 Repose 训练未受影响。

项目根目录：`/home/admin/workspace/xinyu/mulyi-model-tac`。

最终运行：

```text
logs/rsl_rl/dexsuite_revo3_rotate_bulb/2026-09-12_04-42-18_rotate_bulb_scene_reward_20260912T044218Z_resume750_stack256_dual_gpu
```

日志与状态：

```text
remote_train_logs/rotate_bulb_scene_reward_restart_20260912T044218Z/train.log
remote_train_logs/rotate_bulb_scene_reward_restart_20260912T044218Z/latest.json
remote_train_logs/rotate_bulb_scene_reward_restart_20260912T044218Z/launch_manifest.json
```

## 实际更新

- 旋出按实际新增圈数奖励，充分双侧接触时每圈 40 分；三圈进度上限 120，严格完成附加奖励仍为 20。
- 每环境维护历史最高圈数，回退、重新抓取、无接触历史进度及释放后的空载螺杆运动不能重复领分。
- 新增权重 0.5 的连续抓持引导，旋出进度有最多约 0.2 秒线性衰减的接触记忆。成功和搬运仍使用严格双指接触。
- 桌子中心改为 `(0.55, 0, 0.38)`，机械臂根位置改为 `(1, 0, 0.766)`，七轴初始姿态同步补偿以保持原末端位姿。
- 灯座局部 X 改为 `[-0.12, -0.08]`，加上桌子平移后仍处在原来的环境内 X `[0.43, 0.47]` 区域。

服务器最终配置 SHA-256 为
`dc08b9897393a447839dd9679756988b2426132b60d219692c91b977fce3f7d5`，与本地文件完全一致。
共享场景配置、机器人资产配置、灯泡 USD、机器人两个主 USD 与八个子配置层均逐字节一致，无额外资产需要同步。
详细奖励定义见 [接入说明](rotate_bulb_policy_integration.md)。

## 恢复与备份

先在独立过渡运行中部署新奖励，随后按用户确认加入新场景并再次重启。最终从过渡运行已经更新过的
`model_750.pt` 恢复，包含优化器和 actor/critic 归一化状态。这个检查点 SHA-256 前缀为 `6f5daced`，
其优化器步数已从 15100 增至 15120、归一化样本数从 12369920 增至 12386304，模型参数已发生更新。

每卡 512 环境、双卡共 1024；3 圈、30 秒回合；碰撞栈每进程 256 MiB；每 25 轮保存；目标末轮仍为 14999。
最终监督启动 PID 为 27233，仅记录启动时现场，后续操作必须重新核对进程身份。

场景切换前的源文件、检查点、配置、监督与运行清单备份在最终日志控制目录的 `before/` 下。
更早的纯奖励部署备份在 `remote_train_logs/rotate_bulb_reward_restart_20260912T043521Z/before/`。
旧运行和过渡运行均保留，最终训练使用新的 TensorBoard 目录。

## 已完成的本地验证

奖励单元测试 31 项通过；任务注册与触觉/检查点契约测试 9 项通过。服务器也运行了更新后的奖励测试，31 项通过。
实际 GPU 仿真使用新场景和新奖励，2 个环境、完整触觉、默认 3 圈/30 秒，验证 20 次重置、四角位置、
部分重置、末端姿态、被动保持、受控旋出释放、掉落检测及有限奖励/观测，全部通过。
原始记录位于 `logs/diagnostics/rotate_bulb_reward_update_20260912/`。

GitNexus：修改前对奖励类及其方法执行 impact；动态管理器调用返回 UNKNOWN，已通过 RewTerm 与
RewardManager/pytest 调用确认范围。场景配置 impact 为 LOW，上游仅训练和 PLAY 两个入口；未修改严格成功函数。
独立实现和部署依赖审查通过。本次没有提交 Git。

## 最终服务器验证

最终场景与奖励运行完成了 750、751、752 三次 PPO 更新，监督状态为 running。
实际 env.yaml 确认桌子 `(0.55,0,0.38)`、机器人根 `(1,0,0.766)`、七轴补偿初姿、灯座局部 X
`[-0.12,-0.08]`、3 圈/30 秒、每卡 512 环境和新奖励参数均已生效。

两卡均启用 NCCL，每次更新各执行 20 次梯度同步，前三次更新后的最大参数差异均为 0；
首次跨卡梯度均值独立核验通过。三轮日志中的损失和奖励全部有限，non_finite 终止指标为 0。
第 752 轮约 670 steps/s。这是启动与更新正确性验证，不代表策略已经学会新任务。

最终运行首个新 model_750.pt 的 SHA-256 前缀为 `2740cf73`，相对载入检查点的最大参数改变量
约 0.018409；优化器步数从 15120 增至 15140，actor/critic 归一化计数从 12386304 增至
12402688，模型和优化器张量均有限，键及形状保持一致。它写在最终新目录中，原始检查点保留。

最终完整验证、实际配置与资产一致性记录位于：
`logs/diagnostics/rotate_bulb_reward_update_20260912/remote_restart/scene_final/`。
